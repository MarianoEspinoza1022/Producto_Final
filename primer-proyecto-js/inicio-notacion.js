//Notación
//; ---> Delimitar el final de una línea, (no siempre se usa)
//Ejemplo
const a = 1;
a;
console.log(a);

//. --->Se utiliza para acceder a los atributos de un objeto
//Ejemplo
alumno.edad;

//[] --> square brackets
// declarar listas, arrays o vectores
//Ejemplo de declaración
const ar = [1, 2, 3, 4];

//Acceder
ar[2];  //Acceder
console.log(ar[2]);     //Accediendo para imprimir

//() -->Parenthesis function
function promedio(a, b, c, d){
    //Toda la función
}

for(var a = 4, a > if, i++){
    //Bucle
}
if(let a, b, c, d){
    //Función
}

//{} --> Curly Brackets
// me permiten realizar objetos, funciones y estructuras de control
const alumno = {
    edad: 15,
}

for(var a = 4, a > if, i++){
    //Bucle
}

if(let a, b, c, d){
    //Estructura de control
}else{
    //Función
}